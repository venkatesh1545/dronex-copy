import { MobileStream as MobileStreamComponent } from '@/components/MobileStream';

const MobileStream = () => {
  console.log('MobileStream page rendering');
  
  return (
    <div>
      <MobileStreamComponent />
    </div>
  );
};

export default MobileStream;
